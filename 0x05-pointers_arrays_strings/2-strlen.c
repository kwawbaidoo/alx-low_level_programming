#include <stdio.h>
#include "main.h"

/**
 * _strlen - return the length of a string
 * @s: value to return it length
 * Return: not
 */
 int _strlen(char *s)
{
	int strlength = 0;

	strlength = *s;

	_putchar("Length of *s:  %d \n", strlength);
	return (Strlength);
}
