#include <stdio.h>
/**
 * main - Print all single numbers
 * of base 10
 *
 * Return 0
 */
int main(void)
{
	int single_digit;

	for(single_digit = 0; single_digit < 0; single_digit++)
		printf("%i", single_digit);
		putchar('\n');
		return (0);
}

