#include <stdio.h>
/**
 * main - print alphabet in lower and uppercase
 * 
 *
 * Return: 0
 */
int main(void)
{
	char c;
	for (ch ='A'; ch <= 'Z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
